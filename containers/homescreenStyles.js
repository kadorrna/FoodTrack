
import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  description: {
    fontSize: 22,
  },
  button: {
    position: 'absolute',
    bottom:100
  }
})

export default styles
